services:
  - type: web
    name: zorvyn-backend
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: true
      - key: JWT_SECRET
        sync: false
      - key: MONGO_URI
        sync: false
